@model List<Recipe>

<h1>Recipes</h1>

<ul>
    @foreach (var recipe in Model)
    {
        <p>
            @recipe.Name
            <a asp-action="Details" asp-route-id="@recipe.Id">Details</a>
        </p>
    }
</ul>

